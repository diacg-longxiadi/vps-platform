# /etc/caddy/Caddyfile
# 注意：此檔是範例，包含真實域名但無憑證

{
    servers :443 {
        protocols h1
    }
}

docker-vps.xn--acg-4i2f.xyz {
    reverse_proxy localhost:8081
    log {
        output file /var/log/caddy/vps-platform.log
    }
}
